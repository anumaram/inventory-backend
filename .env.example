# MongoDB Atlas Connection String
MONGO_URI=mongodb+srv://karumajjijyothisai24042003_db_user:Jyothisai%402003@cluster0.g9zvpiq.mongodb.net/inventory-app?retryWrites=true&w=majority

# Port (auto-assigned by Render, defaults to 3000 locally)
PORT=3000

# Node Environment
NODE_ENV=production

# JWT Secrets
JWT_SECRET=supersecretjwtkey_change_me_in_production

# Optional: Comma-separated list of additional allowed origins for CORS
# ALLOWED_ORIGINS=https://your-custom-domain.com

